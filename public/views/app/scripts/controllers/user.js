'use strict';

/**
 * @ngdoc function
 * @name condoViewApp.controller:UserCtrl
 * @description
 * # UserCtrl
 * Controller of the condoViewApp
 */
angular.module('condoViewApp')
  .controller('UserCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
